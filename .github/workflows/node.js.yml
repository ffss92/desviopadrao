name: DesvioPadraoCD

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Build and run
        uses: actions/checkout@v3
        run: npm ci
        run: npm run build --if-present
        run: pm2 start npm --name desvio-padrao -- start
